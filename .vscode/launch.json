{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Program with VS Debugger",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/beluga.exe",  // Path to your executable
            "args": [],  // Arguments to pass to the program
            "cwd": "${workspaceFolder}",  // Working directory
            "console": "integratedTerminal",
        }
    ]
}
